# Backend API (FastAPI) — login/logout/refresh go through this API
VITE_API_URL=http://localhost:8000

# Optional: send tenant_id with login when your org requires it
# VITE_AUTH_TENANT_ID=your-tenant-id
